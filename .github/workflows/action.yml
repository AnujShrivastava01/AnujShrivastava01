name: Update Readme.md

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  update-readme:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Dummy step (to keep workflow valid)
        run: echo "action.yml workflow is now valid ✅"
